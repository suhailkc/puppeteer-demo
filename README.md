# Puppeteer demo (Price Tracker)

A simple **price tracker** built with **Node.js** and **Puppeteer** that automatically checks the price of a product (like on Amazon or Flipkart) and notifies you when it drops below your target price.


## Setup

### 1. Install dependencies
```bash
npm install
```

### 2. Run the server
```bash
npm run dev
```